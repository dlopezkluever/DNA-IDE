import { ViewPlaceholder } from '../components/common/ViewPlaceholder'

export function ProteinView() {
  return (
    <ViewPlaceholder
      title="Translation"
      note="Full codon/amino-acid translation view arrives with mutation cross-highlighting (Phase 4)."
    />
  )
}
