import { ViewPlaceholder } from '../components/common/ViewPlaceholder'

export function MapView() {
  return (
    <ViewPlaceholder
      title="Plasmid Map"
      note="Linear and circular feature maps arrive once the GenBank parser lands (Phase 3)."
    />
  )
}
